.header
   = link_to root_path,class: "header-icon"do
      BBOY/BGIRL Community Site
   - if user_signed_in?
      = link_to user_path(current_user)do
         = "#{current_user.gender} #{current_user.name}"
      = link_to "ログアウト", destroy_user_session_path, method: :delete,class: "header-logout"